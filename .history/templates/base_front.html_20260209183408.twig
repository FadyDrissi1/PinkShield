<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="utf-8">
  <meta content="width=device-width, initial-scale=1.0" name="viewport">
  <title>{% block title %}PinkShield - Santé{% endblock %}</title>

  <link href="https://fonts.googleapis.com/css?family=Open+Sans:300,400,600,700|Raleway:300,400,500,600,700" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.0/font/bootstrap-icons.css" rel="stylesheet">

  <style>
    :root { --pink-main: #e83e8c; --pink-hover: #d63384; --pink-light: #fce4ec; }
    body { font-family: "Open Sans", sans-serif; color: #444; background-color: #fef1f6; }
    
    #header { background: #fff; padding: 15px 0; border-bottom: 2px solid var(--pink-light); z-index: 997; }
    
    /* Logo à gauche */
    .logo-container { display: flex; align-items: center; }
    .logo-container img { max-height: 50px; margin-right: 10px; }
    .logo-container a { font-size: 28px; font-weight: 700; color: var(--pink-main); text-decoration: none; }

    .btn-get-started { background: var(--pink-main); color: #fff; border-radius: 50px; padding: 8px 25px; text-decoration: none; transition: 0.3s; }
    .btn-get-started:hover { background: var(--pink-hover); color: #fff; }
  </style>
</head>

<body>
  <header id="header" class="fixed-top">
    <div class="container d-flex align-items-center justify-content-between">
      <div class="logo-container">
        {# Remplace le chemin par celui de ton logo réel #}
        <img src="{{ asset('front/assets/img/logo.png') }}" alt="Logo">
        <a href="/">PinkShield</a>
      </div>
      <nav>
        <a href="/login" class="btn-get-started">Connexion</a>
      </nav>
    </div>
  </header>

  <main id="main" style="margin-top: 120px;">
    {% block body %}{% endblock %}
  </main>
</body>
</html>